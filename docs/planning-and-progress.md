# 规划与开发进度

> **给谁看**：站长回忆进度与下一步；主播偶尔了解大方向；协作开发方对齐任务。  
> **谁来写**：协作开发过程中由 **协作方（含 AI 助手）** 更新本页，站长可随时改文案或勾选状态。  
> **怎么改**：**里程碑、当前焦点** 可 **整体或大段改写**；**更新记录** 在文末 **按日期追加**，与 [头脑风暴记录](brainstorm-log.md) 分工——本页偏 **任务与进度**，头脑风暴偏 **需求与讨论**。

## 当前焦点

- 以 **文档** 固定需求与信息架构；并已启动 **HTML + CSS 前端骨架**（首页 + 录播/听歌/工具/公告占位页）。
- 后续开发默认围绕两条主线推进：**UI好看**（视觉与交互）与 **功能逻辑梳理**（模块边界、数据流、维护方式）。

## 里程碑（粗粒度）

| 阶段 | 状态 | 说明 |
|------|------|------|
| 需求与信息架构 | 进行中 | `product-vision`、`information-architecture`、`brainstorm-log` |
| 站点工程初始化 | 进行中 | `web/` 前端骨架、页面结构、样式与脚本已建立 |
| 核心页面（首页 + 子页占位） | 进行中 | 首页 + 录播/听歌/工具/公告占位页已落地 |
| B 站对接（录播元数据、动态等） | 未开始 | 接口与合规、缓存策略另记 |
| 公告 / 棉花糖等需服务端能力 | 未开始 | 视技术选型拆分 |

## 更新记录

### 2026-04-02

- 新增本文件；与 README「本仓库怎么用」中的三方协作方式对齐；当前阶段仍为文档为主。
- README：补充「与远程仓库同步」约定（会话内酌情 `commit` / `push`）；协作方将此前文档变更集中提交并推送远程。
- README：补充「面向后续网页开发专家接手」的可读性与结构清晰度原则（入口优先、一文一责、先结论后细节、改动留痕）。
- README：明确后续优化主线为「UI好看 + 功能逻辑梳理」，并要求实现与文档双向可追踪，便于专家快速接手。
- 建立第一期最小上线版前端骨架：`web/index.html` + `web/pages/{archive,music,tools,news}.html` + `web/styles` + `web/scripts`。
- README：新增「前端骨架（第一期最小上线版）」与本地预览方法（`python -m http.server 8000`）。
- 品牌命名落地：新增「命名约定」（小时走神了/小时/TimeZone/小时的星球），并将首页文案更新为「TimeZone + 小时的星球」。
- 首页 UI 第一轮优化：强化气泡主视觉、弱化右上角导航权重，加入轻量星际氛围背景、悬停动效与移动端适配（仅样式层改动）。
- 首页 UI 第二轮优化：新增欢迎图占位视觉（星球/星环/星点动效）、调整为「视觉区 + 气泡区」布局；工具页接入 Virtual Keyboard 真实外链。
- 录播页改为日历视图：新增月份切换与按天展示回放条目；首期数据在 `web/scripts/main.js` 的 `VOD_EVENTS` 中维护。
- 录播数据解耦：新增 `web/data/vod-events.json`，录播页改为读取 JSON 渲染日历；读取失败时显示友好提示。
- 录播读取容错增强：JSON 读取失败时自动回退示例数据，并在页面提示使用 `python -m http.server 8000` 进行本地预览。
- 录播容错策略调整：取消示例数据回退，读取失败时仅显示错误提示并渲染空日历，避免影响真实效果判断。
- 录播预览方式固定：仅支持在仓库根目录启动 `python -m http.server 8000` 并访问 `http://localhost:8000/web/pages/archive.html`；`file://` 方式明确提示不支持。
- 录播流程升级为两段式：支持手动维护 `vod-events.json`，并新增 `tools/build_vod_events.py` + `vod-input.json` 自动解析（单条 BV 与系列页尝试）生成事件数据。
- 录播页新增可视化编辑模式：可开启编辑、双击条目修改标题和链接、双击某天新增条目，并支持一键导出 `vod-events.json`。
- 录播解析规则升级：自动从 B 站标题中提取「直播日期 + 直播标题」，日历日期改为直播日期（非上传日期），标题去除“直播回放”与时间后缀。
- 录播覆盖范围修复：系列抓取页数由 2 提升至 30（默认也改为 30），历史数据已扩展到 `2024-12-18` 起。
- 录播自动翻页与分页边界：系列抓取改为默认上限 200 页（自动翻到空页即停）；日历在达到最早/最晚有录播月份时自动禁用「上一页/下一页」按钮。
- 协作流程固化：站长确认后续每次改动均需完成「Git 提交推送 + 服务器同步 + 进度文档留痕」三步。
- 录播页管理员模式：新增登录/退出管理，未登录访客不显示编辑与导出按钮；管理员口令由 `web/data/admin-config.json` 配置。

### 2026-04-03

- 录播页管理入口隐蔽化：访客界面不再显示「访客/管理员」文案与登录按钮；仅通过 `?manage=1` 触发口令，通过后写入会话并自动去掉 URL 参数；口令与可视化操作说明仅在当前标签页的维护会话内展示。
- 录播页新增管理员手动抓取：维护会话内新增「抓取最新录播」按钮，前端调用 `/api/admin/refresh-vod`；新增 `tools/admin_api.py` 在服务器执行 `python3 tools/build_vod_events.py` 并回传结果。
